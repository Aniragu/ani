#include<stdio.h>
int main()
{
int n,m;
printf("\nEnter the number");
scanf("%d",&n);
if(n>0)
printf("\n the given number is positive");
else if(n<0)
printf("\n the given number is negative");
else
printf("\n zero");
return 0;
}
